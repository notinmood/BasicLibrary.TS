export declare class StringHelper {
    /**
     * 获取分隔符前的字符串内容
     * @param wholeString
     * @param separator
     * @returns
     */
    static getContentBeforeSeparator(wholeString: string, separator: string): string;
    /**
     * 获取分隔符后的字符串内容
     * @param wholeString
     * @param separator
     * @returns
     */
    static getContentAfterSeparator(wholeString: string, separator: string): string;
}
